package com.wayapaychat.temporalwallet;

//import java.nio.charset.StandardCharsets;

//import org.springframework.util.Base64Utils;

public class EncryptDecrypt {
	
	/*public static void main(String[] args) throws Exception {
		String input = "How are you?";
		String authHash = Base64Utils.encodeToString(input.getBytes(StandardCharsets.UTF_8));
		System.out.println(authHash);		
		byte[] asBytes = Base64Utils.decodeFromString(authHash);
		String output = new String(asBytes);
		System.out.println(output);
	}*/

}
